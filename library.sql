CREATE TABLE Book (
    ISBN int,
    Title varchar(255),
    Author varchar(255),
    Call_Number varchar(255),
    Pages int
    );

CREATE TABLE Loans (
    ISBN int,
    Patron varchar(255)
    );

